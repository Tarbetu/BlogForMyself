-# .mx-auto.w-full.flex{:class => "md:w-2/3"}
-#   .mx-auto
-#     - if notice.present?
-#       %p#notice.py-2.px-3.bg-green-50.mb-5.text-green-500.font-medium.rounded-lg.inline-block= notice
-#     = render @category
-#     = link_to 'Edit this category', edit_category_path(@category), class: "mt-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium"
-#     .inline-block.ml-2
-#       = button_to 'Delete this category', category_path(@category), method: :delete, data: { confirm: "Are you sure you want to delete this category?" }, class: "mt-2 rounded-lg py-3 px-5 bg-gray-100 font-medium"
-#     = link_to 'Back to categories', categories_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium"

.container
  -# Başa ilgili resim gelecek
  .text-center= render HeaderComponent.new.with_content(@category.name)
  .antialised.text-center.my-6= @category.information
  = render PostListComponent.new(posts: @category.posts)
